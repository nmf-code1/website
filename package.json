{
  "name": "no-mans-fort-website",
  "version": "1.0.0",
  "description": "Static site with optional chat using OpenAI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
